let vetor = ['Caique' , 'Araujo', 'Silva', 'Pereira', 'Severino',
 'Rosa', 'Machado', 'Arruda' ]

//console.log(vetor);

/*
//looping com o vetor
for (var i = 0; i < vetor.length; i++) {
    console.log(`Nome: ${vetor[i]}`);
}


//Foreach For = para each = cada
vetor.forEach(function(valor, indice){
    console.log(`${indice}: ${valor}`)
})
*/

//map

/*
vetor.map(function(val, val2){
    console.log(val2);
});
*/

